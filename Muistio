Sql tiedoston muutokset:

create table goal
 (
     id          int         not null
         primary key,
     name        varchar(40) null,
     probability int         null
 )
     charset = latin1;

 insert into goal (id, name, probability)
 values  (1, 'pikapokeri', 15),
         (2, 'blackjack', 15),
		 (3, 'robber', 5);


blackjack eri pelimekanismi lisäyksiä kuten split tai double

väärä icao ei aiheuta uutta "eventtiä"

kerran jo käyty kenttä sulkeutuu

vegasissa oma peli esim. ruletti
